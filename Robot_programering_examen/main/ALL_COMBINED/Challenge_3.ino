void Program3() {
  //SETUP
   display.clear();
  display.print(F("Change to"));
  display.gotoXY(0, 1);
  display.print("zumo with");
  display.gotoXY(0, 2);
  display.print("prox. sens.");
  display.gotoXY(0, 3);
  display.print("in front");
  
  buttonA.waitForButton();
  //LOOP
  while (true) {
   
  }
}